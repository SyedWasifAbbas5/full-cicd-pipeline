apiVersion: v1
kind: Service
metadata:
  name: cicd-demo
  namespace: cicd-demo

spec:
  type: ClusterIP

  selector:
    app.kubernetes.io/name: full-cicd-pipeline

  ports:
    - name: http
      port: 80
      targetPort: http
